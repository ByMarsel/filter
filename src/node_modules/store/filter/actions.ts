import {action} from "typesafe-actions";
import { FilterState, FilterTypes } from "store/filter/types";


export const set = (filter:FilterState) => action(FilterTypes.SET_FILTER, filter);

export const clean = () => action(FilterTypes.CLEAR_FILTER);


