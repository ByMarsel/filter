import { SelectedValue } from "types";

export interface Range {
  from: number|undefined;
  to: number|undefined;
}

export interface FilterState {
  color: SelectedValue | undefined;
  kind: SelectedValue | undefined;
  priceRange: Range | undefined;
  isNew: SelectedValue | undefined;
}

export enum FilterTypes {
  SET_FILTER = "FilterTypes/SET_FILTER",
  CLEAR_FILTER = "FilterTypes/CLEAR_FILTER"
}
