import { Pony } from "types";

export interface CatalogState {
  items: Pony[];
  isShowFilter: boolean; 
}

export enum CatalogTypes {
  SHOW_FILTER = "CatalogTypes/SHOW_FILTER",
  CLOSE_FILTER = "CatalogTypes/CLOSE_FILTER"
}
